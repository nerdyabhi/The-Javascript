// we can have dynamic keys in object as well

let idType ="samosaCard";

let car ={
    [idType]: "samosa69", 
    noSamosa:40,
}

console.log(idType); // prints samosaCard 
console.log(car[idType]) // prints Samosa69
