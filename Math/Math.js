// TO generate a random no. from range min to max

const min = 10;
const max = 20;

console.log(Math.floor(Math.random() * (max - min + 1) + min));

let num1 = 5 , num2 = 10;

let arr = [1 , 2, 3, 4, 5, 7 , 8];

let maxi = Math.max(...arr);
console.log(maxi)