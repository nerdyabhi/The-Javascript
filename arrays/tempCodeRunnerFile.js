let arr = [];

for (let i = 0; i < 10; i++) arr[i] = i + 1; // woah so much like C

arr.fill(1, 20);

// Methods in javaScript

console.log(arr.length); // prints length of array

console.log(arr.join(' '));
